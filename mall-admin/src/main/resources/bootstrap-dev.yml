spring:
  cloud:
    nacos:
      discovery:
        server-addr: http://localhost:8848
        group: mall-group
      config:
        server-addr: http://localhost:8848
        group: mall-group
        file-extension: yaml