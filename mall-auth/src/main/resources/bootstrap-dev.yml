spring:
  cloud:
    nacos:
      discovery:
        server-addr: http://localhost:8848
        group: mall-group
        cluster-name: mall-cluster
      config:
        server-addr: http://localhost:8848
        group: mall-group
        file-extension: yaml
logging:
  level:
    root: debug