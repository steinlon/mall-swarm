info:
  profile: development
spring:
  datasource:
    url: jdbc:postgresql://localhost:9999/mall
    username: postgres
    password: postgres
logging:
  level:
    root: info
    com.macro.mall: debug
logstash:
  host: localhost